{
  "name": "Project Cypress-23",
  "version": "1.0.0",
  "description": "Just learning tests",
  "main": "index.js",
  "scripts": {
    "test": "cypress open",
    "premyscript": "npx cypress run --spec cypress/e2e/iframe.spec.cy.js --browser electron",
    "myscript": "npx cypress run --spec cypress/e2e/iframe.spec.cy.js --browser chrome",
    "postmyscript": "npx cypress run --spec cypress/e2e/iframe.spec.cy.js --browser edge"

  },
  "author": "Tatiana Valverde",
  "devDependencies": {
    "cypress": "^12.11.0"
  },
  "dependencies": {
    "cypress-file-upload": "^5.0.8"
  }
}
